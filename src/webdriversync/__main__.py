import os
import sys
import argparse
from requests import ConnectionError
from webdriversync import AVAILABLE_DRIVERS as DOWNLOADERS

OS_NAMES = ["mac", "win", "linux"]
BITNESS = ["32", "64"]

def main():
  print("hello webdriver sync")
if __name__ == "__main__":
    main()